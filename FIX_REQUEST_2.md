# RE:TURN — 개선 요청 명세 2차 (FIX_REQUEST_2)

> 사용자가 배포본(`https://webmcp.chocobear159.workers.dev`)을 직접 사용하며 낸 2차 요청 3건과,
> 그 검증 과정에서 발견된 배포 차단 결함 1건.
> 기준 브랜치: `feat/test-track2-policy-realtime` · 기준 커밋: `eeeb5f9`
>
> **§0의 결함은 요청받은 것이 아니라 검증 중에 나온 것이다.** 새 워크스페이스에서 승인이
> 100% 실패하므로 다른 무엇보다 먼저다.

---

## 0. 읽는 법

- 각 항목에 **ID**를 부여했다. 1차 문서(`FIX_REQUEST.md`)의 `FR-` 와 구분해 `FR2-` 를 쓴다.
- **현재 상태**는 전부 코드를 읽고 브라우저·API로 재현한 결과다. 재현하지 못한 것은 그렇게 적었다.
- 1차와 마찬가지로, 증상만 가리는 임시방편은 쓰지 않는다.

### 분류 요약

| 구분 | ID | 성격 |
|---|---|---|
| §0 배포 차단 결함 | FR2-X1 | **요청 외.** 새 워크스페이스에서 승인이 500으로 죽는다 — **완료** |
| §1 큐레이터 케이스 화면 | FR2-K1 · FR2-K2 | 두 버튼이 각각 다른 이유로 작동하지 않는다 — **둘 다 완료** |
| §1 승인 큐 | FR2-K3 | 대기 승인이 2건 이상이면 두 번째부터 열 수단이 없다 — **완료** |
| §2 돋보기 | FR2-M1 | 사진 없는 유물에는 돋보기가 아예 없다 — **완료 (선택지 a)** |
| §2 돋보기 | FR2-M2 | 포인터가 사진 밖으로 나가도 렌즈가 남는다 — **구현 완료** |
| §3 자산 표시·내려받기 | FR2-D1 · FR2-D2 | 파일이 공개 화면에 나오지 않고, 눌러도 받을 수 없다 — **둘 다 완료** |

---

## 0. FR2-X1 · 새 워크스페이스에서 승인이 500으로 죽는다 — **최우선**

**증상 — 재현함.** Fresh workspace에서 승인을 해소하면 예외 없이 실패한다. 시드 승인이든
새로 만든 승인이든 같다.

```
POST /api/curator/approvals/APR-004-museum_<uuid>/resolve  →  500
D1_ERROR: LIKE or GLOB pattern too complex
  at app/api/curator/approvals/[id]/resolve/route.ts:219
```

**근본 원인 — 확인함.** `resolve/route.ts:67` 의 legacy 링크 조건이 **컬럼 값으로 LIKE 패턴을
만든다.**

```sql
submissions.id LIKE a.result || '-museum_%'
```

순수 SQLite(`node:sqlite`)에서는 이 패턴들이 전부 통과한다. 터지는 것은 **D1 이 LIKE 패턴
길이를 훨씬 좁게 제한**하기 때문이다. 패턴이 활동 로그 행마다 새로 만들어지므로, 워크스페이스
안에 긴 `result` 값이 **하나만 있어도** 문장 전체가 실패한다.

| 워크스페이스 | 가장 긴 LIKE 패턴 | 승인 해소 |
|---|---:|---|
| `museum_demo_01` (짧은 id) | **32자** | 200 OK |
| `museum_3fd842ec-…` (UUID id) | **61자** | **500** |

새 워크스페이스는 id 가 UUID 라 시드 활동 로그의 `result` 가 이미
`SUB-1042-museum_<uuid>` (52자)다. 거기에 `-museum_%` 가 붙어 61자가 되고 한계를 넘는다.
**`museum_demo_01` 만 id 가 짧아서 우연히 통과하고 있었고**, 그래서 기존 워크스페이스에서
돌린 smoke 는 이 경로를 통과했다.

**영향** — 데모 Scene 5·6이 fresh museum 에서 동작하지 않는다. 영상 촬영은 fresh museum 으로
시작하므로 그대로 두면 촬영이 불가능하다. `smoke` 도 1건 실패한다
(`publishing updates every evidence-linked contribution atomically`).

**요청** — LIKE 를 걷어낸다. 의도는 "시드 제출물 id = `{원본id}-{museumId}`" 라는 **정확한
문자열 관계**이므로 와일드카드가 필요 없다.

```sql
a.result = submissions.id
  OR submissions.id = a.result || '-' || submissions.museum_id
```

**함께 고칠 곳** — `db/queries.ts:154` `listSubmissionEvidenceIds` 에 같은 구문이 있다.

```sql
(result=? OR ? LIKE result || '-museum_%')
→ (result=? OR ?=result || '-' || ?)      -- submissionId, submissionId, museumId
```

**완료 조건** — 새 워크스페이스에서 리셋 직후 시드 승인을 해소하면 200 이 돌아오고 라벨
리비전이 올라간다. `smoke` 가 두 번 연속 전부 통과한다.

---

## 1. 큐레이터 케이스 화면의 두 동작

`/curator/cases/[id]` 하단의 두 버튼이다. **원인이 서로 다르므로 따로 다룬다.**

### FR2-K1 · Request clarification — 질문이 기여자에게 닿지 않는다

**현재 상태 — 재현함.** 버튼 자체는 동작한다. 케이스 상태가 `received` → `needs information`
으로 바뀌고 화면도 갱신된다. **문제는 그 다음이다.**

큐레이터가 쓴 질문은 **활동 로그의 detail 문자열에만** 들어간다.
`app/api/curator/submissions/[id]/clarify/route.ts:19`

```ts
await recordActivity(museumId, 'Mina, Curator', 'requested clarification',
  `${submission.title} · ${question}`, { ... });
```

제출물 자체에는 아무것도 저장되지 않는다. 그래서 기여자 쪽에서 **질문 원문을 볼 수 없다.**

| 기여자가 보는 것 | 실제 내용 |
|---|---|
| `check_submission` 도구 응답 | `"A curator asked a follow-up question about this contribution."` |
| `/submissions/[id]` 페이지 | `"A curator needs more information before review can continue."` |
| 질문 원문 등장 횟수 | **0회** (페이지 HTML 실측) |

큐레이터는 질문을 썼는데 기여자는 무엇을 물었는지 알 수 없다. 되묻는 고리가 끊겨 있다.

**부수 문제** — 한 번 보내면 버튼이 영구히 비활성화된다
(`disabled={status === 'needs information'}`). 두 번째 질문을 보낼 수 없고, 케이스 화면에서
**이전에 무엇을 물었는지 다시 볼 수도 없다.**

**요청**

- 질문을 제출물에 **저장**하고, 기여자 상태 페이지와 `check_submission` 에서 **원문을 보여준다.**
- 케이스 화면에도 보낸 질문 이력이 남아, 큐레이터가 무엇을 물었는지 다시 볼 수 있게 한다.
- 두 번째 질문을 보낼 수 있게 한다. 비활성화가 아니라, 이미 보낸 질문을 보여주고
  추가 질문을 허용하는 형태가 맞다.

> 저장 위치를 정해야 한다. 후보: `submissions` 에 `clarification` 컬럼 신설 /
> 별도 `clarifications` 테이블(질문·답변 왕복을 담을 수 있음) / `details` JSON 안에 편입.
> 기여자 답변까지 받을 계획이면 별도 테이블이 맞고, 단방향 고지로 충분하면 컬럼이 싸다.

### FR2-K2 · Review proposed update — 대기 승인이 없으면 아무 일도 일어나지 않는다

**현재 상태 — 재현함.** 대기 승인이 0건일 때 버튼을 눌러도 **아무 반응이 없다.** 드로어도
열리지 않고, 메시지도 없고, 페이지도 그대로다.

| 요소 | 대기 승인 0건일 때 |
|---|---|
| 상단바 `Pending approval` | `0 No pending approval`, **disabled** — 상태를 정확히 알린다 |
| 케이스 화면 `Review proposed update` | **활성 상태 그대로**, 눌러도 무반응 |

**근본 원인 — 확인함.** 세 겹이다.

1. 버튼은 `window.dispatchEvent(new Event('open-approval'))` 만 쏜다
   (`components/curator/evidence-desk-actions.tsx:47`). 성공·실패를 알 방법이 없다.
2. `CuratorShell` 은 그 이벤트로 `setDrawer(true)` 를 하지만, 렌더 조건이
   `{drawer && approval && (...)}` (`curator-shell.tsx:259`) 이다. `approval` 이 없으면
   **조용히 아무것도 그리지 않는다.**
3. `approval` 은 `app/curator/layout.tsx:19` 의 **`pending[0]`** — 워크스페이스 전체의 첫 번째
   대기 승인이다. **보고 있는 케이스와 무관하다.**

3번은 0건일 때보다 오히려 더 나쁜 경우를 만든다. 대기 승인이 **다른 유물** 것이면, 케이스 A를
보다가 버튼을 누르면 **유물 B의 라벨 개정안이 열린다.** 지금은 데모 워크스페이스의 대기 승인이
둘 다 moonbird-mask 라 우연히 가려져 있다.

**요청**

- 버튼을 **케이스에 연결**한다. 이 케이스의 유물에 걸린 대기 승인을 열고, 없으면 열 것이 없다고
  말한다. 다른 유물의 승인을 여는 일은 없어야 한다.
- 열 것이 없으면 **버튼을 비활성화**하거나, 눌렀을 때 왜 열리지 않는지 말한다. 상단바 트리거는
  이미 그렇게 하고 있으므로 같은 규칙을 따르면 된다.
- 케이스 화면 하단 `Agent draft with evidence references` 에는 개정안이 그려져 있는데, 그것을
  승인 요청으로 만드는 경로가 화면에 없다. **"이 초안을 승인 대기로 올린다"** 동작이 필요한지
  결정해야 한다(§4 참조).

---

### FR2-K3 · 대기 승인이 2건 이상이면 두 번째부터 열 수 없다

**현재 상태 — 재현함.** 승인 2건을 만들어 콘솔을 열었다.

| | |
|---|---|
| 상단바 배지 | `2 Pending approval` — **개수는 사실을 말한다** |
| 드로어가 보여주는 것 | `APR-8A9E0502-E5F` 하나뿐 |
| 나머지로 가는 수단 | **없다.** 목록도, 다음/이전도, select 도 없다. 버튼은 `×` · `Reject` · `Approve` 뿐 |

큐레이터는 "2건이 기다린다"는 말을 듣고 **하나만 볼 수 있다.** 두 번째는 첫 번째를 처리한
뒤에야 나타난다. 사용자가 "pending approval 이 반영이 안 되는 것 같다"고 한 것이 이것이다 —
숫자는 갱신되는데 내용이 따라오지 않는다.

**실시간 자체는 정상이다.** 콘솔을 열어둔 채 밖에서 승인을 만들면 배지가 **964ms** 만에
`0 No pending approval` → `1 Pending approval` 로 바뀐다. 해소할 때 1 → 0 이 되는 것도
배포본에서 확인했다. 갱신 경로가 아니라 **드로어가 큐의 머리 하나만 다룬다는 것**이 문제다.

**근본 원인** — FR2-K2 와 같은 뿌리다. `app/curator/layout.tsx:19` 의 `pending[0]`.
큐가 아니라 단일 값으로 다루고 있다.

**요청** — 대기 승인이 여러 건일 때 큐레이터가 **어느 것이든 열 수 있게** 한다. 드로어 안의
목록이든, 승인 페이지든, 케이스별 연결(FR2-K2)이든 형태는 판단에 맡긴다. 다만 배지가 N 을
말하면 N 개에 도달할 수 있어야 한다.

> FR2-K2 와 함께 고치는 편이 자연스럽다. 케이스에 연결하면 "이 케이스의 승인" 이라는 축이
> 생기고, 그것만으로 머리 하나 문제도 대부분 사라진다.

---

## 2. FR2-M1 · 사진이 없는 유물의 돋보기 — **완료**

**있었던 문제 — 확인함.** `components/community/object-gallery.tsx` 가 사진이 없으면
갤러리 전체를 렌더하지 않고 대체 이미지만 내보냈다. 돋보기 버튼은 갤러리 안에 있으므로
함께 사라졌다. 시드 유물 8개에는 사진이 없으니, 기여 → 공개를 거친 유물 외에는 어디서도
돋보기를 쓸 수 없었다.

**결정 — 사용자가 (a) 선택.** 대체 이미지에도 돋보기를 붙인다.

**구현** — 배경 이미지 방식은 쓸 수 없다. 대체 이미지는 파일이 아니라 CSS 로 그린 도형이라
샘플링할 원본이 없기 때문이다. 대신 **렌즈 안에 같은 도형을 한 번 더 렌더**하고 변환을 건다.

- 배율 `2.8` — 사진 렌즈의 `background-size: 280%` 와 같은 값이라 두 렌즈가 같은 크기로 확대된다
- 복제본의 원점을 렌즈 중앙에 두고, 스케일된 포인터 오프셋만큼 이동시킨다.
  이렇게 하면 **렌즈의 픽셀 크기가 계산에 들어가지 않아** CSS 의 반응형 렌즈 크기와 무관하게 맞는다
- 포인터·키보드 규칙은 사진 렌즈와 동일하다. 이탈 시 사라지고, 키보드 조작 중에는 유지된다
- 대체 이미지에는 내려받을 파일이 없으므로 Download 링크를 내보내지 않는다

**정직하게 적어둔 것** — 그려진 도형은 확대해도 **새 정보가 나오지 않는다.** 화면 안내 문구가
"illustration is drawn rather than photographed, so magnifying it enlarges the same detail
rather than revealing more" 라고 말하고, 컴포넌트 주석도 같은 말을 한다. 컬렉션 전체가
같은 방식으로 동작하게 하는 것이 목적이고, 돋보기가 값을 하는 곳은 실제 사진이 있는 기록이다.

**검증 — 브라우저 실측**

| 항목 | 결과 |
|---|---|
| 사진 없는 유물 | 갤러리·돋보기 버튼 렌더됨, 화살표·점·Download 없음 |
| 렌즈 배율 | 페이지의 도형 582px → 렌즈 안 1629px = **2.8배** |
| 포인터 지점 정확도 | (0.25,0.75)·(0.8,0.2)·(0.5,0.5) 에서 렌즈 중앙과 **1–3px** 오차(테두리·반올림) |
| 포인터 이탈 / 복귀 | 사라짐 → 34.9% 에서 이어짐 |
| 키보드 | 34.9% → 46.9%, 이탈해도 유지, Escape 로 닫힘 |
| 사진 경로 회귀 | 배경 이미지 방식 유지, 복제 요소 없음, 캐러셀·Download 정상 |

---
### FR2-M2 · 포인터가 사진 밖으로 나가면 렌즈를 감춘다 — **구현 완료**

**요청** — 돋보기를 켠 상태에서 마우스가 사진 밖으로 나가면 렌즈가 사라져야 한다.

**이전 동작** — 1번 트랙의 `6c5ec9f` 가 "마지막 위치를 유지" 로 만들어 두었다. 그 커밋은
내가 넣었던 "중앙으로 복귀" 회귀를 고친 것이고, 그때는 마지막 위치 유지가 합리적인 선택이었다.
사용자는 감추는 쪽을 원했다.

**구현** — `components/community/object-gallery.tsx`

- 렌즈를 마지막으로 움직인 입력이 무엇인지 `drivenBy` ref 로 기억한다
- 포인터가 프레임을 벗어나면(`onMouseLeave`) **포인터가 몰고 있었을 때만** 렌즈를 지운다
- **키보드 사용 중에는 지우지 않는다.** 화살표로 렌즈를 옮기는 사람의 포인터는 페이지 아무
  곳에나 있을 수 있고, 우연히 프레임 경계를 넘을 수도 있다. 그때 렌즈를 치우면 다른 이동
  수단이 없는 사람에게서 돋보기를 빼앗는 셈이 된다
- `zooming` 은 그대로 유지되므로 돋보기는 켜진 채다. 포인터가 다시 들어오면 그 지점에서 이어진다

**검증 — 브라우저 실측**

| 경로 | 결과 |
|---|---|
| 켜기 | 렌즈 50%, 프레임에 포커스 |
| 포인터 이동 | 71.96% 로 추적 |
| **포인터 이탈** | **렌즈 사라짐**, `aria-pressed` 는 `true` 유지 |
| 포인터 복귀 | 39.8% 에서 즉시 이어짐 |
| 키보드 이동 | 50% → 62% (ArrowRight 2회) |
| **키보드 중 포인터 이탈** | **62% 그대로 유지** |
| Escape | 닫힘, `aria-pressed` false, `tabIndex` -1 |

---

## 3. 기여한 자산의 표시와 내려받기

### FR2-D1 · 사진 외의 파일은 공개 화면에 아예 나오지 않는다

**현재 상태 — 확인함.** `app/objects/[id]/page.tsx:77`

```ts
const publicImageRows = assetRows
  .filter((row) => row.kind === 'image')
```

`document`(PDF)와 `audio` 는 **여기서 걸러진다.** 공개 유물 페이지 어디에도 문서·오디오를
내보내는 경로가 없다(해당 파일 안에 `document`/`audio`/`download` 언급 0회).

기여자가 문서나 녹음을 올리고 큐레이터가 공개해도, **공개 기록에는 존재하지 않는 것과 같다.**
업로드 라우트는 PDF·오디오를 정상으로 받고, 자산 제공 라우트도 정상으로 내보낸다. 끊긴 곳은
공개 페이지 한 곳뿐이다.

**요청** — 공개된 문서·오디오도 유물 페이지에 표시한다. 사진 캐러셀과는 성격이 다르므로
별도 목록이 맞다(파일명·종류·크기·기여 출처).

**제약** — 표시 여부는 반드시 `assetAccess` 를 통과한 것만. `restricted`·`sealed` 와 consent 가
`private` 인 자산은 목록에도 나오면 안 된다.

### FR2-D2 · 눌러도 내려받을 수 없다

**현재 상태 — 확인함.** 두 갈래다.

| 자산 | `content-disposition` | 화면에서 |
|---|---|---|
| 이미지 | `inline` | 캐러셀에 `<img>` 로만 그려진다. **링크가 아니라 클릭 대상이 아니다** |
| 문서·오디오 | `attachment; filename="…"` | **화면에 나오지 않으므로 누를 것이 없다** (FR2-D1) |

`app/api/assets/[id]/route.ts:35` 는 이미 비이미지에 `attachment` 를 붙이고 있다. 즉 **서버는
이미 내려받기를 지원하고, 화면에 링크가 없을 뿐이다.**

**요청** — 사진과 파일을 눌러 내려받을 수 있게 한다.

**정해야 할 것** — 이미지의 `content-disposition` 은 `inline` 이어야 캐러셀에 그려진다.
내려받기를 같이 지원하려면 둘 중 하나다.

| 선택지 | 성격 |
|---|---|
| (a) 별도 내려받기 링크 + `?download=1` 쿼리로 disposition 전환 | 캐러셀 표시는 그대로 두고 명시적 버튼을 둔다. 서버 한 줄 분기 |
| (b) `<a download>` 속성만 사용 | 같은 origin 이라 동작하지만, 파일명 제어가 약하고 브라우저마다 다르다 |

> (a)를 권한다. 파일명이 서버가 아는 원본명으로 나가고, 이미지·문서가 같은 규칙을 쓴다.
> 어느 쪽이든 `assetAccess` 판정은 그대로 통과해야 한다.

---

## 4. 열려 있는 결정

| ID | 결정할 것 | 상태 |
|---|---|---|
| FR2-K1 | 질문 저장 위치 | **결정함** — `submissions.clarifications` JSON 목록. 새 테이블 없이 질문 여러 건을 담는다. 기여자 **답변**은 요청되지 않아 넣지 않았고, 필요해지면 그때 테이블로 옮기면 된다 |
| FR2-K2 | 케이스 초안을 승인 요청으로 올리는 동작 | **만들지 않음** — 보고된 결함은 "버튼이 죽어 있다"이지 "새 동작이 없다"가 아니다. 범위를 넓히지 않았다. 필요하면 별도 요청으로 |
| FR2-K3 | 여러 승인을 고르는 위치 | **결정함** — 드로어 안 목록. 새 화면을 만들지 않고, 케이스 연결(K2)과 함께 두 경로를 모두 덮는다 |
| FR2-M1 | 돋보기가 없는 유물 | **사용자가 (a) 선택.** 대체 이미지에도 돋보기를 붙였다. 확대해도 새 정보가 나오지는 않는다는 점을 화면 문구와 코드 주석에 남겼다 |
| FR2-D2 | 내려받기 방식 | **결정함** — `?download=1`. 서버가 아는 원본 파일명이 나가고, 이미지·문서가 같은 규칙을 쓴다 |

---

## 5. 권장 순서

```
0. FR2-M2        렌즈가 포인터와 함께 사라짐    ← 완료
1. FR2-X1        새 워크스페이스 승인 500      ← 완료
2. FR2-K2 + K3   죽은 버튼 · 케이스 연결 · 큐  ← 완료
3. FR2-D1·D2     파일 표시와 내려받기          ← 완료
4. FR2-K1        clarification 왕복            ← 완료
5. FR2-M1        돋보기가 없는 유물             ← 완료
```

---

## 6. 검증 원칙

1차와 동일하게 유지한다.

- `npm run verify` 와 `npm run test:smoke` 를 **따로** 실행한다.
- **`test:smoke` 는 두 번 연속 통과해야 한다.** 유물 등록 검사가 영구 레코드를 만들기 때문에
  리셋 이후 새 워크스페이스에서 돌도록 배치돼 있다.
- **FR2-X1 은 반드시 fresh workspace 에서 확인한다.** `museum_demo_01` 은 id 가 짧아 이 결함을
  가린다. 이것이 지금까지 아무도 눈치채지 못한 이유다.
- UI 변경은 화면을 실제로 렌더해 확인한다. 브라우저 창 폭도 함께 본다 — 460px 에서는
  `.table-head` 가 `display:none` 이라 정렬 측정이 무의미하다.
- 자산에 닿는 변경은 `restricted`·`sealed`·`private` 이 공개 경로로 나가지 않는지 회귀로 고정한다.

### 문서 동기화

| 시점 | 고칠 문서 |
|---|---|
| FR2-X1 완료 시 | `PROCESS.md` (원인과 재현 조건) |
| FR2-K1 완료 시 | `SCHEMA.md` (저장 위치) · `WEBMCP_TOOLS.md` (`check_submission` 응답) |
| FR2-D1·D2 완료 시 | `RETURN_PLAN.md` §14.3 · `SCHEMA.md` 불변식 |
| FR2-M1 완료 시 | `RETURN_PLAN.md` §14.3 · §3.4 (시드 이미지를 도입한 경우) |
