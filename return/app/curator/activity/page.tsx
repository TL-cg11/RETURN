import { activities } from '@/lib/demo-data';
export default function ActivityPage(){return <main className="console-page"><div className="page-head"><div><p className="console-eyebrow">Audit trail</p><h1>Activity</h1><p>Agent research, policy decisions, and human edits remain visible as one record.</p></div></div><div className="audit-log">{activities.map((a,i)=><article key={i}><time>26 AUG<br/><strong>{`19:${42-i*4}`}</strong></time><span className={`actor-mark actor-${i}`}>{a.actor.split(' ').map(x=>x[0]).join('').slice(0,2)}</span><div><strong>{a.actor}</strong><p>{a.action}</p><small>{a.detail}</small></div><i>{i===2?'Denied':i===4?'Pending':'Applied'}</i></article>)}</div></main>}
