/**
 * Backward-compatible type exports for policy modules.
 * Runtime demo records now live in D1 and are queried through db/queries.ts.
 */
export type { AssertionMode, Authority, Consent, Visibility } from './domain/types';
